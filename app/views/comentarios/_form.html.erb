<%= form_with(model: [post, comentario]) do |form| %>
  <div class="form-control">
    <%= form.rich_text_area :body %>
    <%= form.submit submit_label, class: "btn btn-primary mt-1" %>
  </div>
<% end %>